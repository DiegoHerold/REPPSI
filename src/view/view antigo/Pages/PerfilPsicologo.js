const React = require('react');
const ProfilePageP = require('../components/ProfilePageP');
const Header = require('../components/HeaderPerfil');

  const PerfilPsicologo = () => {
    return (
        <>
        <Header />
        <ProfilePageP/>
        </>
      );
  };
  
  module.exports = PerfilPsicologo;